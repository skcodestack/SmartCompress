<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skcodestack.imageselect" >
    <application>
        <activity android:name=".ImageSelectActivity" android:screenOrientation="portrait" android:theme="@style/AppThemeNoTitle"></activity>
        <activity android:name=".ImagePerviewActivity" android:screenOrientation="portrait" android:theme="@style/AppThemeNoTitle"></activity>
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.skcodestack.imageselect"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>
</manifest>
